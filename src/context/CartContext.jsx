import { createContext } from 'react';

// We just create the empty context here.
// You manage the state, the logic, and the Provider entirely inside App.jsx!
export const CartContext = createContext();
