import { createContext } from 'react';

// We create the empty Wishlist context here.
// You manage the state, logic, and Provider entirely inside App.jsx!
export const WishlistContext = createContext();
